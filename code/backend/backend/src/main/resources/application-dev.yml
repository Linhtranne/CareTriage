spring:
  datasource:
    url: jdbc:h2:mem:caretriage;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: linhtranne
    password: 123456
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  sql:
    init:
      mode: always

app:
  jwt:
    secret: caretriage-super-secret-key-that-is-at-least-256-bits-long-for-hs256
    expiration-ms: 86400000
    refresh-expiration-ms: 604800000

  ai-service:
    internal-api-key: Z9jzNTEwSMh4hQBsHLi0RBsW5fl995ZNOYEMHMckE4E=

  ai:
    runtime: python
    gemini:
      api-key: ${GEMINI_API_KEY:}
      model-name: ${GEMINI_MODEL_NAME:gemini-2.5-pro}
      embedding-model-name: ${GEMINI_EMBEDDING_MODEL:text-embedding-004}
      temperature: 0.1
      timeout-seconds: 60
